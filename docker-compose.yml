version: '3.8'

services:
  elana-app:
    container_name: elana-academy-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
